#pragma once

/** Initializes CTF spawn feature cvars. */
void CtfSpawn_InitCvars();

/** Returns whether custom respawn logic is enabled (_sofbuddy_custom_respawn != 0). */
bool CtfSpawn_CustomRespawnEnabled();